GEMINI_API_KEY=your_key_here
# Optional: USE_DEEP_RESEARCH=1 to use Gemini Deep Research for Stage 0E (Exploratory; web-backed, slower)
# Optional: APP_PASSCODE=your_secret_passcode — if set, users must enter this to run the pipeline (for private deployments)
